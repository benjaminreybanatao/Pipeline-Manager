# Copy to backend/.env to override the defaults.
DATABASE_URL=postgresql+psycopg://postgres@127.0.0.1:5432/pipeline
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Days in a stage before a deal is flagged as slow / stalled on the board.
STAGE_WARNING_DAYS=21
STAGE_STALE_DAYS=45
